This folder contains endpoints to manage Reflect itself.

---

These endpoints can be called using the `reflect/` prefix in an ordinary Reflect request.

*Internal request example using `API->call()`*

```php
$this->call("reflect/Endpoint?id=ping", Method::GET);
```
